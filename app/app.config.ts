export default defineAppConfig({
  ui: {
    colors: {
      primary: "violet",
      neutral: "zinc",
    },
    button: {
      slots: {
        base: "cursor-pointer",
      },
    },
    link: {
      base: "cursor-pointer",
    },
  },
});